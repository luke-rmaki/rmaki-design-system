import styled from 'styled-components';

export const SwatchCollection = styled.ul`
  display: flex;
  flex-wrap: wrap;
  margin: none;
  list-style-type: none;
  width: 100%;
  justify-content: space-around;
  padding: 0;
`;
